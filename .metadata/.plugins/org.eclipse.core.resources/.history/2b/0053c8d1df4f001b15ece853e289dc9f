package com.master.jm;

public interface MasterIfs_01 {
	// 보다시피 interface는 private 을 쓸수없다. public 이나 final로 해주어야한다.
	//private int intKor;
	public int intEng();
	public int intMath();
	int intEng(int intCount);
}
